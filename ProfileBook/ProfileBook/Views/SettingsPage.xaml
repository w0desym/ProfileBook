<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             x:Class="ProfileBook.Views.SettingsPage"
             Title="{Binding Title}">

    <StackLayout Padding="30">
        <!--<buttons:SfRadioGroup>
            <buttons:SfRadioButton
                Text="Light Theme"
                IsChecked="{Binding IsLightThemeChecked}"/>
            <buttons:SfRadioButton
                Text="Dark Theme"
                IsChecked="{Binding IsDarkThemeChecked}"/>
        </buttons:SfRadioGroup>-->
        <!--<Button BackgroundColor="{AppThemeBinding Light={StaticResource LightNavigationBarColor}, Dark={StaticResource DarkNavigationBarColor}}"/>-->
        <StackLayout>
            <Label 
                Text="Sorting"
                FontSize="Title"/>
            <buttons:SfRadioGroup>
                <buttons:SfRadioButton
                    Text="By Nickname"
                    FontSize="20"
                    IsChecked="{Binding IsNicknameChecked}"/>
                <buttons:SfRadioButton
                    Text="By Name"
                    FontSize="20"
                    IsChecked="{Binding IsNameChecked}"/>
                <buttons:SfRadioButton
                    Text="By Date"
                    FontSize="20"
                    IsChecked="{Binding IsDateChecked}"/>
            </buttons:SfRadioGroup>
        </StackLayout>
        <StackLayout>
            <Label 
                Text="Theme"
                FontSize="Title"/>
            <!--#region Theme-->
            <!--#endregion-->
        </StackLayout>
        <StackLayout>
            <Label 
                Text="Localization"
                FontSize="Title"/>
            <!--#region Localization-->
            <!--#endregion-->
        </StackLayout>
    </StackLayout>

</ContentPage>